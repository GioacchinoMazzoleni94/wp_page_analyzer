// static/js/main.js
import { bindUI, startAnalysis } from './handlers.js';

document.addEventListener('DOMContentLoaded', () => {
  bindUI();
  // Se vuoi avviare subito l’analisi con valori di default, puoi chiamare:
  // startAnalysis();
});
